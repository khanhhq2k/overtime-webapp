<div class="site-logo">
  <h1>
    Ovetime Tracker
  </h1>
</div>
<% if current_user %>
  <ul class="nav nav-tabs custom-nav">
    <li class="<%= active?(root_path) %>">
      <%= link_to "Home", root_path %>
    </li>
    <li class="<%= active?(posts_path) %>">
      <%= link_to "Time Entries", posts_path %>
    </li>
    <li class="<%= active?(new_post_path) %>">
      <%= link_to "Add New Entry", new_post_path, id: 'new_post_btn' %>
    </li>
    <li class="dropdown pull-right">
       <a href="#" data-toggle="dropdown" class="dropdown-toggle">Account<strong class="caret"></strong></a>
      <ul class="dropdown-menu">
        <% if ADMIN_USER_TYPES.include? current_user.try(:type) %>
          <li>
            <%= link_to "ToDO: Admin Dashboard", root_path %>
          </li>
          <li class="divider"></li>
        <% end %>
        <li>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
        </li>
      </ul>
    </li>
  </ul>
<% end %>
